import { ButtonProps, GridProps } from "@chakra-ui/react";

const styles = {
  grid: {
    as: "section",
    flex: "1",
    gridTemplateColumns: "1fr 1fr",
    gap: "6.25rem",
    alignContent: "center",
  } as GridProps,
};

export default styles;
