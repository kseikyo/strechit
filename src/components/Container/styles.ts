import { FlexProps } from "@chakra-ui/react";

const styles = {
  container: {
    flexDir: "column",
    h: "100vh",
    maxWidth: "992px",
    mx: ["auto"],
    py: [10],
    px: [8],
  } as FlexProps,
};

export default styles;
